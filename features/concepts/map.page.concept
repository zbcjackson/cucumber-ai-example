Concept: MapPage
    Behavior: search for a location "{{name}}"
        browser: input [[name]] into the search box
        browser: click the search button
        browser: wait for the search results populates

    Behavior: verify the address "{{address}}"
        browser: check the address shows "[[address]]"